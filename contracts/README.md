# API contracts

`api/openapi.yaml` documents the browser-facing API. `schema/` contains the
machine-readable JSON Schemas used by the BFF and mirrored by backend models.
Contract changes should be reviewed before implementation changes.
