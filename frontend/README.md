# Calorie Compass frontend

React, TypeScript, Vite, MUI, React Hook Form, and Zod client for Calorie
Compass. The browser calls only `/api`; Vite proxies that path to the BFF in
development. See the repository root `README.md` for setup and architecture.

